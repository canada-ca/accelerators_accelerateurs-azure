{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "containerSasToken": {
            "type": "string"
        },
        "vmArray": {
            "type": "array",
            "metadata": {
                "description": "Array of VM objects."
            }
        }
    },
    "variables": {
        "baseVMUrl": "[concat(uri(deployment().properties.templateLink.uri, 'decryptVMDisks.json'), parameters('containerSasToken'))]"
    },
    "resources": [
        {
            "name": "[concat('vmGroupdeploy', parameters('vmArray')[copyIndex()].vmName)]",
            "type": "Microsoft.Resources/deployments",
            "apiVersion": "2017-05-10",
            "resourceGroup": "[parameters('vmArray')[copyIndex()].vmResourceGroup]",
            "dependsOn": [],
            "copy": {
                    "name": "vmObj",
                    "count": "[length(parameters('vmArray'))]"
            },
            "properties": {
                "mode": "Incremental",
                "parameters": {
                    "vmObject": { "value": "[parameters('vmArray')[copyIndex()]]" }
                },
                "templateLink": {
                    "uri": "[variables('baseVMUrl')]",
                    "contentVersion": "1.0.0.0"
                }
            }  
        }
    ],
    "outputs": {}
}